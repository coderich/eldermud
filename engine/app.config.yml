redis:
  url: 'redis://localhost:6379'
  namespace: 'eldermud'

action:
  login:
    map: '${self:map.town}'
    room: '${self:map.town.rooms.start}'
  map:
    coords:
      n: [0, 2, 0]
      s: [0, -2, 0]
      e: [2, 0, 0]
      w: [-2, 0, 0]
